# ─────────────────────────────────────────────────────────────────────────────
# NAMUNA (template) — bu faylni `env.txt` deb nusxalang va qiymatlarni to'ldiring.
#
#   cp env.example.txt env.txt   (yoki Windows: copy env.example.txt env.txt)
#
# DIQQAT: env.txt ILOVAGA qadoqlanadi (pubspec.yaml → assets). Shuning uchun
# bu yerda HECH QANDAY MAXFIY KALIT BO'LMASLIGI KERAK.
#
# Barcha maxfiy kalitlar (Qwen, Cerebras, Mistral, Gemini, OneSignal REST,
# AWS) Cloudflare Worker ichida (Worker Secrets) saqlanadi va ilova ularni
# faqat proksi orqali ishlatadi.
# ─────────────────────────────────────────────────────────────────────────────

# Proksi (barcha maxfiy so'rovlar shu orqali o'tadi)
CF_WORKER_URL=https://your-worker.example.workers.dev

# Ilova → proksi autentifikatsiya tokeni. Worker'dagi APP_TOKEN bilan bir xil
# bo'lishi shart.
APP_TOKEN=REPLACE_WITH_YOUR_APP_TOKEN

# Model nomlari (maxfiy emas)
QWEN_CHAT_MODEL=qwen3.5-122b-a10b
QWEN_GAME_MODEL=qwen-plus-latest
CEREBRAS_MODEL=gpt-oss-120b
MISTRAL_MODEL=mistral-small-latest
GEMINI_MODEL=gemini-2.0-flash

# OneSignal ilova identifikatori — maxfiy EMAS (mijoz SDK'sida ham bor).
ONESIGNAL_APP_ID=REPLACE_WITH_YOUR_ONESIGNAL_APP_ID

# AWS Polly regioni (maxfiy emas)
AWS_REGION=eu-central-1

# Cloudinary — faqat ommaviy (public) qiymatlar.
CLOUDINARY_CLOUD_NAME=REPLACE_WITH_YOUR_CLOUD_NAME
CLOUDINARY_UPLOAD_PRESET=ml_default
